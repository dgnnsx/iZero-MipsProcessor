module multiplexador_jal(RT, RD, ctrl_mux_br, regEscrito);
	// Entradas
	input [4:0] in; // Registrador a ser escrito
	input ctrl;
	
	// Saida
	output [4:0] out;
	
	assign out = ctrl ? 5'b11111 : in;
endmodule